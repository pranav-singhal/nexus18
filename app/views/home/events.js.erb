var container = $('#masonry-container')
var box = $('.card');

container.masonry('remove',box);
<%@events.each do |event|%>
console.log("event added");
var node = "<%=escape_javascript(render("event", event: event))%>";
// console.log(node);

container.prepend(node).masonry('reload');
<%end%>
